@{
    ViewData["Title"] = "Email Verification";
}

<div class="container mt-5">
    <div class="card shadow p-4 text-center">
        <h2 class="text-success">Email Verification</h2>
        <p>@ViewBag.Message</p>
        <a href="@Url.Action("Login", "Auth")" class="btn btn-primary">Go to Login</a>
    </div>
</div>